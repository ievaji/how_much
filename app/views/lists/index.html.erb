<h2>Lists Index</h2>

<%= form_tag do %>
  <ul>
    <% @lists.each do |li| %>
      <li>
        <%= check_box_tag 'li_ids[]', li.id %>
        <%= li.window.name.first(3) %> :: <%= li.name %>
      </li>
    <% end %>
  </ul>
  <%= button_to 'Save' %>
<% end %>
